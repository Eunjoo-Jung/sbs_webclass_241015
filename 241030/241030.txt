position 속성!

- 레이아웃 정렬 x

> html, css 영역

**진짜 진짜 개중요함 **
1) 전후문자선택자
2) 공간분할태그
3) 포지션 속성
** 자다가도 튀어 나와야함 ** 

- static  : 정적인 <-> 동적인
   > html태그를 입력한 순서에 맞춰서 순서대로 좌표를 유지하는 것!!
   > html에서 default (*기본값)

- relative : 상대적인 <-> 절대적인
   > 원래 static 값을 가지고 있는 현상태를 기준으로 *상대적으로 어떤 *좌표값인지를 결정

- absolute : 절대적인
   > 현재 사용자가 보고있는 웹 브라우저 화면을 기준으로 절대적인 좌표값 부여
   > 예외사항!!!
       만약, 절대적인 위치값을 주고 싶긴 하나, 특정 요소를 기준으로 한 상태에서 절대값을 적용하고 싶은 경우!!! 

- fixed : 고정된
   > 다른 요소들이 스크롤바에 의해서 위치가 이동될 때, 유일하게 고정된 좌표값을 적용받는 요소! 

- sticky : 부착되어있는 (*sticker) -->> 요즘 트렌드 (와 신기함!!)
   > 특정 위치에 도달하기 전까지는 기존 static 속성을 그대로 유지하다가 해당 특정 위치에 도달하면 그때에는 스티커처럼 부착되는 형태를 유지

- overflow : hidden
   > 특정 구역을 정의해놓고 해당 구역 바깥에 있는 요소는 보이지 않게 하고자 할 때
   > overflow : scroll -> 스크롤바 생성되어서 스크롤해서 볼 수 있게 함

- z-index
   > 만약 position 속성값으로 absolute(*절대적인) 좌푯값이 여러개 존재할 때, 어떤 요소를 최우선으로 나타나게 하고, 후순위로 어떤 요소를 나타나게 하고자 할 때
   > 수를 크게 할수록 더 위로 올라가도록 함

float 
웹디자인기능사같은 구닥다리 시험.. ㅋㅋ....에서만 쓴댕..
옛날 방식의 레이아웃 정렬

!! 실전에서 써먹을 수 있는 서비스를 만드는 게 제일 좋음.!! 
- 알고리즘 // 자료구조 이해!!
- 시간복잡도 (*빅오)-> 코드 효율성 체크 -> 페이지가 로드되는 시간을 체크함
- 코드 & 성장가능성 & 인성 & UI/UX


