.name { }
{ } => 해야할 일 / : 은는이가 / ; ~다.

변수선언
char ch; 문자명 변수를 선언
int n; 정수 선언
float f; 실수 선언
----------------------------------> 하지만 자바스크립트는 이게 필요없음
var 라고 선언하면 된다.

ex) document.write( );
document -> 객체
. -> 접근/참조의 의미
write( ) -> 함수 or 메소드

<br> -> 문장 줄 바꾸기


